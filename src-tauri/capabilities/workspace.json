{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "workspace",
  "description": "Allow the trusted local Balto workspace to call native Balto commands",
  "local": false,
  "remote": {
    "urls": ["http://127.0.0.1:3080/*"]
  },
  "windows": ["main"],
  "permissions": ["core:default"]
}
